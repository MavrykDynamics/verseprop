{
  "name": "mavryk-verseprop-contracts",
  "version": "1.0.0",
  "description": "",
  "devDependencies": {
    "@types/jest": "^28.1.8",
    "@types/mocha": "^9.1.1",
    "@types/node": "^20.3.1",
    "@types/yargs": "^17.0.10",
    "assert": "^2.0.0",
    "chai": "^4.3.6",
    "chai-as-promised": "^7.1.1",
    "chai-bignumber": "^3.0.0",
    "fs-extra": "^10.1.0",
    "lodash": "^4.17.21",
    "onchange": "^7.1.0",
    "promise-retry": "^2.0.1",
    "rimraf": "^3.0.2"
  },
  "scripts": {
    "pull-sandbox": "TAG=4.4.0 docker-compose -f docker-compose.yml pull",
    "start-sandbox": "TAG=4.4.0 docker-compose -f docker-compose.yml up -d && bash ./sandbox-init.sh",
    "start-sandbox-m1": "docker run --rm --platform=linux/arm64/v8 --name mavryk-sandbox -e block_time=3 --detach -p 8732:20000 oxheadalpha/flextesa:20230203 limabox start && bash ./sandbox-init.sh",
    "down-sandbox": "docker-compose -f docker-compose.yml down",
    "clear-sandbox": "docker-compose -f docker-compose.yml down -v",
    "restart-sandbox": "yarn clear-sandbox && yarn start-sandbox",
    "generate-error-codes": "bash ./generate-error-codes.sh",
    "compile": "./bash-compile-contracts.sh -c",
    "deploy": "bash ./bash-deploy-contracts.sh -c",
    "test": "bash ./bash-test-contracts.sh -c",
    "test-net-deploy": "NODE_ENV=development NETWORK_TO_MIGRATE_TO=ghostnet yarn deploy all"
  },
  "author": "tim@mavrykdynamics.io",
  "license": "MIT",
  "dependencies": {
    "@taquito/signer": "^16.0.0",
    "@taquito/taquito": "^16.0.0",
    "@taquito/tezbridge-signer": "^14.2.0",
    "@taquito/utils": "^16.0.0",
    "@types/expect": "^24.3.0",
    "assert": "^2.0.0",
    "convert-string": "^0.1.0",
    "mocha": "^10.0.0",
    "sha256": "^0.2.0",
    "ts-mocha": "^10.0.0",
    "ts-node": "^10.8.1",
    "typescript": "^4.7.4",
    "yargs": "^17.5.1"
  }
}
