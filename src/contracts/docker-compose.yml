version: '3.6'
services:
  db:
    image: timescale/timescaledb:latest-pg15
    shm_size: 1g
    restart: always
    environment:
      - POSTGRES_USER=root
      - POSTGRES_PASSWORD=root
      - POSTGRES_DB=bcd
      - PGDATA=/var/lib/postgresql/data/pgdata
    volumes:
      - db:/var/lib/postgresql/data
    logging: &bcd-logging
      options:
        max-size: 10m
        max-file: '5'

  api:
    restart: always
    platform: linux/amd64
    image: ghcr.io/mavryk-network/bcdhub-api:4.7.7
    environment:
      - BCD_ENV=sandbox
      - GIN_MODE=debug
      - POSTGRES_USER=root
      - POSTGRES_PASSWORD=root
      - SANDBOX_NODE_URI=http://sandbox:20000
      - SANDBOX_IPFS_GATEWAY=https://cloudflare-ipfs.com
    depends_on:
      - db
    ports:
      - 127.0.0.1:14000:14000
    volumes:
      - bcdshare:/etc/bcd
    links:
      - 'flexmasa:sandbox'
    expose:
      - 14000/tcp
    logging: *bcd-logging

  indexer:
    restart: always
    platform: linux/amd64
    image: ghcr.io/mavryk-network/bcdhub-indexer:4.7.7
    environment:
      - BCD_ENV=sandbox
      - POSTGRES_USER=root
      - POSTGRES_PASSWORD=root
      - SANDBOX_NODE_URI=http://sandbox:20000
      - SANDBOX_IPFS_GATEWAY=https://cloudflare-ipfs.com
    depends_on:
      - db
    links:
      - 'flexmasa:sandbox'
    volumes:
      - bcdshare:/etc/bcd
    logging: *bcd-logging

  flexmasa:
    container_name: mavryk-sandbox
    restart: always
    image: registry.gitlab.com/mavryk-network/flexmasa:atlas-update-run
    command: atlasbox start
    environment:
      - block_time=3
      - flexmasa_node_cors_origin=*
    ports:
      - 127.0.0.1:20000:20000
      - 127.0.0.1:8732:20000
    expose:
      - 20000/tcp
      - 8732/tcp
    logging: *bcd-logging

  gui:
    container_name: sandbox-gui
    restart: always
    platform: linux/amd64
    image: ghcr.io/mavryk-network/bcdhub-gui:4.7.7
    depends_on:
      - api
    ports:
      - 127.0.0.1:8000:80
    logging: *bcd-logging

volumes:
  bcdshare:
  db:
    driver: local
